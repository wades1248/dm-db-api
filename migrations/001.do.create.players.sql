CREATE TABLE players (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    level INTEGER NOT NULL, 
    pp INTEGER NOT NULL,
    ac INTEGER NOT NULL,
    dmid TEXT NOT NULL 
)